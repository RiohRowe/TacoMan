const SearchBar = () => {
    return (
        <div>
            <label>Search for a customer</label>
            <input type={"text"}/>
            <button type={"submit"}>search</button>
        </div>
    );
};

export default SearchBar;